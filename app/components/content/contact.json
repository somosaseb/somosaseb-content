{
  "collectionName": "components_content_contacts",
  "info": {
    "name": "Contact",
    "icon": "external-link-alt"
  },
  "options": {},
  "attributes": {
    "type": {
      "type": "enumeration",
      "enum": [
        "phone",
        "whatsapp",
        "websiteUrl",
        "twitterHandle",
        "instagramHandle",
        "linkedinProfileUrl",
        "facebookPageUrl"
      ]
    },
    "value": {
      "type": "string",
      "required": true
    },
    "visibility": {
      "type": "enumeration",
      "enum": [
        "public",
        "members",
        "staff",
        "admins"
      ],
      "default": "members",
      "required": true
    }
  }
}
